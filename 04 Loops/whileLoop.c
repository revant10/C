#include <stdio.h>

void main()
{
    // printing first 20 natural numbers

    int i = 1;
    
    // while loop first checks the condition and
    // then executes the block if condition is true
    while (i <= 20)
    {
        printf("%d\n", i);
        i++;
    }
}